# CLAUDE.md

Read and follow [AGENTS.md](./AGENTS.md) — it is the single source of truth for working in this repository (scope rule, entry format, validation commands).

Key points: this is a multi-agent AI safety awesome list; single-agent safety content is out of scope; verify every link before adding it; run `npm run validate` before committing.
